<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="community">
	<select id="exprReview" resultType="com.koitt.jardin.dto.community.PreUserDTO">
		select * from preuser where preUserNo=#{preUserNo}
	</select>
	
<!-- 	체험단 글 보기 뷰-->
	<select id="exprView" resultType="com.koitt.jardin.dto.community.PreUserDTO">
		select * from preuser where preuser_no=#{preuser_no}
	</select>
<!-- 	체험단 신청 글 쓰기 -->
	<insert id="preUserReview" >
	insert into preuserReview 
	</insert>
	
	<insert id="preUserApply" >
	insert into preuser_apply values(PREUSER_APPLY_SEQ.nextval,#{preuser_no},#{id},#{title},#{content})
	</insert>   		

   <!-- comment 페이징,검색 -->
<!--    	 comment 전체 게시글 수 -->
	<select id="commentPageNation" resultType="com.koitt.jardin.dto.page.PageNationDTO">
		select count(*) listCnt from review where thumbnail is null
	</select>
	 <!-- comment 전체 검색 게시글 수 -->
	<select id="commentPageNationTo" resultType="com.koitt.jardin.dto.page.PageNationDTO">
		select count(*) listCnt from review  where thumbnail is null and title like '%'|| #{search}||'%' or content like '%'|| #{search}||'%' 	
	</select>
	 <!-- comment 제목 제목검색 게시글 수 -->
	<select id="commentPageNationTi" resultType="com.koitt.jardin.dto.page.PageNationDTO">
		select count(*) listCnt from review  where thumbnail is null and title like '%'|| #{search}||'%' 	
	</select>
	 <!-- comment 내용 내용검색 게시글 수 -->
	<select id="commentPageNationCo" resultType="com.koitt.jardin.dto.page.PageNationDTO">
		select count(*) listCnt from review  where thumbnail is null and content like '%'|| #{search}||'%' 	
	</select>

	<!-- comment페이징 글 리스트 가져오기 -->
	<select id="commentPageNationList" resultType="com.koitt.jardin.dto.community.EpilogueDTO">
		select * 
      from(select row_number() over(order by review_no desc) as rnum, review_no,product_no,id,title,content,review_date,hit,grade,thumbnail
      from review where thumbnail is null order by review_no desc)
      where rnum between #{curPage}*10-9 and #{curPage}*10
   </select>
   
   <!-- comment 전체 검색 글 리스트 가져오기 -->
   <select id="commentPageNationToList" resultType="com.koitt.jardin.dto.community.EpilogueDTO">

		select * 
      from(select row_number() over(order by review_no desc) as rnum, review_no,product_no,id,title,content,review_date,hit,grade,thumbnail 
      from review where thumbnail is null and title like '%'|| #{search}||'%' or content like '%'|| #{search}||'%' 
       order by review_no desc)
      where rnum between #{curPage}*10-9 and #{curPage}*10

   </select>
   
   <!-- comment 제목 검색 글 리스트 가져오기 -->
   <select id="commentPageNationTiList" resultType="com.koitt.jardin.dto.community.EpilogueDTO">
		select * 
      from(select row_number() over(order by review_no desc) as rnum, review_no,product_no,id,title,content,review_date,hit,grade,thumbnail 
      from review  where thumbnail is null and title like '%'|| #{search}||'%'
       order by review_no desc)
      where rnum between #{curPage}*10-9 and #{curPage}*10
   </select>
   
   <!-- comment 내용 검색 페이징 글 리스트 가져오기 -->
   <select id="commentPageNationCoList" resultType="com.koitt.jardin.dto.community.EpilogueDTO">
		select * 
      from(select row_number() over(order by review_no desc) as rnum, review_no,product_no,id,title,content,review_date,hit,grade,thumbnail 
      from review  where thumbnail is null and content like '%'|| #{search}||'%'
       order by review_no desc)
      where rnum between #{curPage}*10-9 and #{curPage}*10
   </select>
   	 
</mapper>